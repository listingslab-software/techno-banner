import React from 'react';
// import { render } from '@testing-library/react';
//import App from '../App';

test('The sun will rise', () => {
  // render(<App />);
  // const linkElement = screen.getByText(/learn react/i);
  const sunWillRise = true
  expect(sunWillRise).toBe(true)
});
